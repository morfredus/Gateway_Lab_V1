; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
description  = Gateway Lab V1 - Découverte et inventaire des appareils sur le réseau local
default_envs = esp32s3_n16r8

[env:esp32s3_n16r8]
platform      = espressif32
board         = esp32-s3-devkitc-1-n16r8v
framework     = arduino
monitor_speed = 115200
upload_speed  = 921600

board_build.usb_mode        = "cdc"
board_build.usb_cdc_on_boot = 1

; Génère include/web_interface*.h depuis web_src/ avant compilation.
; Commande manuelle : python tools/minify_web.py
; (les fichiers générés sont versionnés — pas de pre-script requis)

build_unflags =
    -std=gnu++11
    -std=gnu++14

build_flags =
    -std=gnu++17
    -D PROJECT_VERSION='"0.0.4"'
    -D PROJECT_NAME='"GatewayLabV1"'
    -D TARGET_ESP32_S3

lib_deps =
    bblanchon/ArduinoJson@^7.0.0
