// Auto-generated by tools/minify_web.py — NE PAS MODIFIER MANUELLEMENT
// Source : web_src/scan.html
#pragma once

static const char SCAN_PAGE[] PROGMEM = R"HTML(
<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Gateway Lab V1 — Équipements</title><style>*{box-sizing:border-box;margin:0;padding:0;}body{font-family:'Segoe UI',system-ui,sans-serif;background:#0f172a;color:#e2e8f0;min-height:100vh;display:flex;flex-direction:column;align-items:center;padding:0 1rem 2rem;}nav{width:100%;max-width:700px;display:flex;gap:0.25rem;padding:0.75rem 0;border-bottom:1px solid #ffffff0a;margin-bottom:1.5rem;}nav a{font-size:0.8rem;padding:0.35rem 0.8rem;border-radius:6px;text-decoration:none;color:#94a3b8;transition:background 0.15s,color 0.15s;}nav a:hover{background:#1e293b;color:#e2e8f0;}nav a.active{background:#0c4a6e;color:#38bdf8;font-weight:600;}h1{font-size:1.5rem;font-weight:700;color:#06b6d4;margin-bottom:0.25rem;}.subtitle{font-size:0.8rem;color:#94a3b8;margin-bottom:1.5rem;}.card{background:#1e293b;border-radius:12px;padding:1.5rem;width:100%;max-width:700px;box-shadow:0 4px 24px rgba(0,0,0,0.3);margin-bottom:1.5rem;}.card-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem;flex-wrap:wrap;gap:0.5rem;}.card-title{font-size:0.7rem;text-transform:uppercase;letter-spacing:0.12em;color:#64748b;font-weight:600;}.card-meta{font-size:0.75rem;color:#94a3b8;}.scan-btn{font-size:0.8rem;padding:0.4rem 1rem;background:#0891b2;color:#fff;border:none;border-radius:7px;cursor:pointer;font-weight:600;transition:background 0.2s;}.scan-btn:hover{background:#06b6d4;}.scan-btn:disabled{background:#334155;color:#64748b;cursor:default;}.progress-wrap{width:100%;background:#0f172a;border-radius:6px;height:4px;margin-bottom:1rem;display:none;}.progress-bar{height:100%;background:#06b6d4;border-radius:6px;width:0;transition:width 0.5s;}.table-wrap{overflow-x:auto;}table{width:100%;border-collapse:collapse;font-size:0.8rem;min-width:500px;}th{text-align:left;color:#94a3b8;font-size:0.65rem;text-transform:uppercase;letter-spacing:0.08em;padding:0.5rem 0.5rem;border-bottom:1px solid #ffffff14;white-space:nowrap;}td{padding:0.55rem 0.5rem;border-bottom:1px solid #ffffff0a;color:#e2e8f0;vertical-align:middle;}tr:last-child td{border-bottom:none;}tr:hover td{background:#ffffff06;}.ip-cell{font-weight:700;color:#f1f5f9;font-family:monospace;font-size:0.82rem;white-space:nowrap;}.mac-cell{font-family:monospace;font-size:0.72rem;color:#94a3b8;white-space:nowrap;}.name-cell{color:#7dd3fc;}.vendor-badge{font-size:0.65rem;background:#1e3a5f;color:#93c5fd;border-radius:4px;padding:0.15rem 0.45rem;white-space:nowrap;}.seen-cell{color:#94a3b8;white-space:nowrap;}.empty-msg{color:#94a3b8;font-size:0.8rem;text-align:center;padding:1.5rem 0;}.scan-info{font-size:0.75rem;color:#38bdf8;margin-bottom:0.75rem;}</style></head><body><nav><a href="/">Accueil</a><a href="/scan" class="active">Équipements</a><a href="/update">OTA</a></nav><h1>Équipements réseau</h1><p class="subtitle" id="scan-subtitle">—</p><div class="card"><div class="card-header"><div><p class="card-title">Équipements détectés</p><p class="card-meta" id="scan-meta">Aucun scan effectué</p></div><button class="scan-btn" id="scan-btn" onclick="triggerScan()">Scanner</button></div><div class="progress-wrap" id="progress-wrap"><div class="progress-bar" id="progress-bar"></div></div><p class="scan-info" id="scan-info" style="display:none"></p><div class="table-wrap"><table><thead><tr><th>IP</th><th>Nom</th><th>Fabricant</th><th>MAC</th><th>Vu il y a</th></tr></thead><tbody id="devices-body"><tr><td colspan="5" class="empty-msg">Cliquez sur "Scanner" pour détecter les équipements</td></tr></tbody></table></div></div><script>var pollTimer = null; var progressVal = 0; var progTimer = null; function fmtSeen(ms) { var s = Math.floor((Date.now() - ms) / 1000); if (s < 5) return 'maintenant'; if (s < 60) return s + 's'; if (s < 3600) return Math.floor(s / 60) + ' min'; return Math.floor(s / 3600) + 'h'; } function renderDevices(devices) { var tbody = document.getElementById('devices-body'); var meta = document.getElementById('scan-meta'); if (!devices || devices.length === 0) { tbody.innerHTML = '<tr><td colspan="5" class="empty-msg">Aucun équipement détecté</td></tr>'; meta.textContent = '0 équipement'; return; } meta.textContent = devices.length + ' équipement' + (devices.length > 1 ? 's' : '') + ' détecté' + (devices.length > 1 ? 's' : ''); devices.sort(function(a, b) { var pa = a.ip.split('.').map(Number); var pb = b.ip.split('.').map(Number); for (var i = 0; i < 4; i++) { if (pa[i] !== pb[i]) return pa[i] - pb[i]; } return 0; }); tbody.innerHTML = devices.map(function(d) { var name = d.hostname || '<span style="color:#334155">—</span>'; var vendor = d.vendor ? '<span class="vendor-badge">' + d.vendor + '</span>' : '<span style="color:#334155">—</span>'; return '<tr>' + '<td class="ip-cell">' + d.ip + '</td>' + '<td class="name-cell">' + name + '</td>' + '<td>' + vendor + '</td>' + '<td class="mac-cell">' + d.mac + '</td>' + '<td class="seen-cell">' + fmtSeen(d.lastSeen) + '</td>' + '</tr>'; }).join(''); } function animateProgress() { if (progressVal < 90) { progressVal += (progressVal < 50) ? 2 : 0.5; document.getElementById('progress-bar').style.width = progressVal + '%'; } } function startProgressAnim() { progressVal = 0; document.getElementById('progress-wrap').style.display = 'block'; document.getElementById('progress-bar').style.width = '0%'; progTimer = setInterval(animateProgress, 200); } function stopProgressAnim() { clearInterval(progTimer); document.getElementById('progress-bar').style.width = '100%'; setTimeout(function() { document.getElementById('progress-wrap').style.display = 'none'; }, 600); } function fetchDevices() { fetch('/api/devices') .then(function(r) { return r.json(); }) .then(function(data) { renderDevices(data.devices); var btn = document.getElementById('scan-btn'); var info = document.getElementById('scan-info'); var sub = document.getElementById('scan-subtitle'); if (data.scanning) { btn.disabled = true; btn.textContent = 'Scan en cours…'; info.style.display = 'block'; info.textContent = '⟳ Résolution des équipements en cours — les résultats s\'affichent progressivement'; sub.textContent = 'Scan en cours…'; if (!pollTimer) pollTimer = setInterval(fetchDevices, 2000); } else { btn.disabled = false; btn.textContent = 'Scanner'; info.style.display = 'none'; stopProgressAnim(); sub.textContent = data.devices && data.devices.length ? data.devices.length + ' équipement(s) — Actualisé ' + new Date().toLocaleTimeString('fr-FR') : 'Aucun équipement détecté'; if (pollTimer) { clearInterval(pollTimer); pollTimer = null; } } }); } function triggerScan() { document.getElementById('scan-btn').disabled = true; document.getElementById('scan-btn').textContent = 'Démarrage…'; startProgressAnim(); fetch('/api/scan', { method: 'POST' }) .then(function() { fetchDevices(); }); } fetchDevices(); setInterval(function() { if (!pollTimer) fetchDevices(); }, 60000);</script></body></html>
)HTML";
